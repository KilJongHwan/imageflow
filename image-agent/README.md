# Image Agent

`image-agent` is reserved for background image processing workers or model-serving code.

Suggested next steps:
- define the runtime (`Python`, `Node.js`, etc.)
- decide how jobs are consumed (`Redis`, queue, webhook)
- document the contract with `backend`
